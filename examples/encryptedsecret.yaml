apiVersion: sync.lumos.io/v1alpha1
kind: EncryptedSecret
metadata:
  name: app-secrets
  namespace: default
spec:
  storeRef:
    name: my-git-store
    kind: ConfigStore
  ageKeyRef:
    name: age-key
  refreshInterval: 5m
  data:
    - source: secrets/app.enc.yaml
  target:
    name: app-secret
